     ветка SQL_update ?
database.py >>> обновить методы обращения на новые, большая часть функций... сделать работу с объектами, а не словарями (возвращать можно и словари)
    обновить методы алхимии? select.where.scalars вместо query.filter (database.getLastPrices() - пример)
    flush при добавлении книг в дб
    переход со словарей на объекты из модели? (пример database._getAllBooks())
        выглядит перспективно, написал функцию в модели, которая выдает словарь по объекту, ровно как генерируемые вручную... разница так то не большая, но прикольно
    
models.py >>> проверить медот возвращения словарей... возможно нужно добавить модель для базы юзверей? 

getDriver.py >>> тут пока все норм, его я обновлял для оптимизации на убунту

utils.py >>> очень большой файл, переделать в модуль с файлами поменьше, или хай так будет? !utils/*

ozon_parser.py >>> фильтровать результаты, если на один товар в разных типах с одним артикулем - оставлять один (приоритет art>isbn>text)

plots.py >>> в целом нормально, выделением цвета когда цена равна и минимальной и последней иногда не работает... 
    добавить вариант с сортированным по цене списком

tgBotOzon.py  >>> не закончено много функций - в процессе

прочие модули
    файл инициализация всего - создание пустой бд, папок, импорт из csv если он есть, создание расписания
    бэкап и проверка базы с востановлением
    логи (? aiologger loguru)
    плотсы в таблицы, с сортировкой и прочим? гугл док, хтмл, минисайт/вебприложение?
    асинхронность?  разные бд для разных пользователей?



КОММЕНТ:
строка поиска книжек https://ozon.kz/category/knigi-16500/?sorting=price&text=